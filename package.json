{
  "name": "dede_es1c",

  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-postbuild": "cd webapp && npm install --include=dev && npm build && cd ../restapi && npm install --include=dev",
    "start": "cd restapi && npm run dev"
  },
  "engines": {
    "node": "16.x",
    "npm": "6.x"
  }
}
