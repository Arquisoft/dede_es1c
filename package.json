{
  "name": "dede_es1c",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-postbuild": "cd webapp && npm install --only=dev && npm install && cd ../restapi && npm install --only=dev && npm install",
    "start": "cd webapp && npm run start"
  },
  "engines": {
    "node": "16.x",
    "npm": "6.x"
  }
}
