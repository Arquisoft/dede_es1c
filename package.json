{
  "name": "dede_es1c",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-postbuild": "cd webapp && npm install && npm install --only=dev && cd ../restapi && npm install && npm install --only=dev",
    "start": "cd webapp && npm run start"
  },
  "engines": {
    "node": "16.x",
    "npm": "6.x"
  }
}
