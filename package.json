{
  "name": "dede_es1c",

  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-postbuild": "cd webapp && npm install --also dev && npm run build && cd ../restapi && npm install --also dev",
    "start": "cd restapi && npm run dev"
  },
  "engines": {
    "node": "16.x",
    "npm": "6.x"
  }
}
