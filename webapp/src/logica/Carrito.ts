/* import { CarroProducto } from "../shared/shareddtypes";
 */
export function calcularTotal(gastoenvio: number): number {
    let total: number = 0;
    /*
    products.forEach((p: CarroProducto) => {
      total += p.unidad*p.producto.price;
    });
    total += gastoenvio;
    */
    return total;
  }
  export function calcularNumeroProductosCarrito(): number {
    let total: number = 3;
    
    return total;
  }